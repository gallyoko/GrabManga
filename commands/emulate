if [ "${1}" == "android" ] || [ "${1}" == "ios" ]
then
    echo "${1} emulation launched... please waiting"
    echo ""
    ionic cordova emulate ${1} >> log/emulate.log
else
    echo "usage : emulate android|ios"
fi